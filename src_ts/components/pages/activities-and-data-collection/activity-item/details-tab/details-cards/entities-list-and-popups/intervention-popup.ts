import {html, LitElement, TemplateResult} from 'lit-element';

export class InterventionPopup extends LitElement {
  // language=HTML
  render(): TemplateResult {
    return html`
      <etools-dialog> </etools-dialog>
    `;
  }
}
