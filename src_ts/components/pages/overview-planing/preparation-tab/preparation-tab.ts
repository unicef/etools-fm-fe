import { customElement, html, LitElement, TemplateResult } from 'lit-element';

@customElement('preparation-tab')
export class PreparationTab extends LitElement {

    // main template
    // language=HTML
    public render(): TemplateResult {
        return html``;
    }
}
